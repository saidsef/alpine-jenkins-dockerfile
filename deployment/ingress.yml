---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: jenkins
  namespace: cicd
spec:
  rules:
  - host: jenkins.saidsef.co.uk
    http:
      paths:
      - path: /
        backend:
          serviceName: jenkins
          servicePort: 80